// Public surface of @diffusecraft/ui.
// v1 ships the theme module + the components barrel (added in Spec 2,
// `ui-component-library`). Components land in waves (Forms first;
// Layout / Overlays / Pickers follow).

export * from './theme';
export * from './components';
