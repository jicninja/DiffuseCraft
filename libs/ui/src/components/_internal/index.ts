// Internal barrel — NOT re-exported from the public surface (`libs/ui/src/index.ts`).
// Components inside `libs/ui/src/components/` import from here only.

export { cn, TextClassContext } from './utils';
export { Icon } from './icon';
